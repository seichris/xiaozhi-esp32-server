# Docker安装Server

version: '3'
services:
  xiaozhi-esp32-server:
    image: ghcr.io/xinnan-tech/xiaozhi-esp32-server:server_latest
    container_name: xiaozhi-esp32-server
    restart: always
    security_opt:
      - seccomp:unconfined
    environment:
      - TZ=Asia/Shanghai
      - XIAOZHI_LLM_BASE_URL=${XIAOZHI_LLM_BASE_URL:-}
      - XIAOZHI_LLM_MODEL_NAME=${XIAOZHI_LLM_MODEL_NAME:-hermes-agent}
      - XIAOZHI_LLM_API_KEY=${XIAOZHI_LLM_API_KEY:-dummy}
      - XIAOZHI_LLM_PROVIDER_NAME=${XIAOZHI_LLM_PROVIDER_NAME:-HermesLLM}
      - XIAOZHI_INTENT_MODULE=${XIAOZHI_INTENT_MODULE:-nointent}
    command:
      - sh
      - -lc
      - |
        mkdir -p /opt/xiaozhi-esp32-server/data
        if [ ! -s /opt/xiaozhi-esp32-server/data/.config.yaml ] || [ "$(tr -d '[:space:]' < /opt/xiaozhi-esp32-server/data/.config.yaml)" = "{}" ]; then
          printf 'server:\n  websocket: wss://xiaozhi.8o.vc/xiaozhi/v1/\n  vision_explain: https://xiaozhi.8o.vc/mcp/vision/explain\nselected_module:\n  ASR: FunASRServer\n' > /opt/xiaozhi-esp32-server/data/.config.yaml
        fi
        exec python app.py
    # ports:
      # ws服务端
      # - "8000:8000"
      # http服务的端口，用于简单OTA接口(单服务部署)，以及视觉分析接口
      # - "8003:8003"
    volumes:
      # 配置文件目录
      - xiaozhi-runtime-data:/opt/xiaozhi-esp32-server/data
      # 模型文件挂接，很重要
      - ./models/SenseVoiceSmall/model.pt:/opt/xiaozhi-esp32-server/models/SenseVoiceSmall/model.pt
volumes:
  xiaozhi-runtime-data:
